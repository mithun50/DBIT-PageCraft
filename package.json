{
  "name": "dbit-pagecraft",
  "version": "1.0.0",
  "private": true,
  "description": "DBIT/VTU academic document generator - PWA, fully client-side",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "node scripts/build.js && npx serve --listen 3000",
    "start": "npx serve --listen 3000"
  },
  "engines": {
    "node": ">=16"
  }
}
